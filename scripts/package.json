{
  "name": "@agent-group/scripts",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prepare": "effect-language-service patch",
    "claude-fast-mode-probe": "bun run claude-fast-mode-probe.ts",
    "claude-haiku-thinking-probe": "bun run claude-haiku-thinking-probe.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-group/contracts": "workspace:*",
    "@agent-group/shared": "workspace:*",
    "@anthropic-ai/claude-agent-sdk": "^0.2.77",
    "@effect/platform-node": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/language-service": "catalog:",
    "@effect/vitest": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
